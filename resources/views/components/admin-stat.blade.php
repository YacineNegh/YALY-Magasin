@props(['label', 'value'])

<div class="rounded-lg border border-brand-blush bg-white p-5 shadow-sm">
    <p class="text-xs font-bold uppercase tracking-wide text-brand-coral">{{ $label }}</p>
    <p class="mt-3 text-3xl font-extrabold tracking-tight">{{ $value }}</p>
</div>
